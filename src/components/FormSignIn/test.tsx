import { render, screen } from '@testing-library/react'

import FormSignIn from '.'

describe('<FormSignIn />', () => {
  it('should render the form', () => {
    // verifique o TextField email
    //verifique password
    //verifique button
  })

  it('should render the forgot link', () => {})

  it('should render the text to sign up', () => {
    // verificar o texto
    // verificar o link
  })
})
